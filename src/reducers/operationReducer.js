import { types } from '../types/types';

const initialState = {
	data: [], // operations
};

export const operationReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};
